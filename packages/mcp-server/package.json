{
  "name": "@libra-design/mcp-server",
  "version": "0.1.0",
  "description": "Libra MCP Server — 向 AI Agent 暴露设计令牌和组件生成。Go 原生版单二进制 10ms 冷启动，npm 版在本包。",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "libra-mcp": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@libra-design/tokens": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0"
  },
  "keywords": [
    "libra",
    "mcp",
    "design-tokens",
    "design-system"
  ],
  "license": "MIT"
}
