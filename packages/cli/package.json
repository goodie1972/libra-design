{
  "name": "@libra-design/cli",
  "version": "0.1.0",
  "description": "Libra CLI — init/add 组件安装。Go 原生版单二进制零依赖（go-cli），npm 版也在本包。",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "libra": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "libra",
    "cli",
    "design-system",
    "components"
  ],
  "license": "MIT",
  "dependencies": {
    "commander": "^12.0.0",
    "fs-extra": "^11.0.0"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.0",
    "@types/node": "^20.0.0",
    "vite": "^5.4.0"
  }
}
